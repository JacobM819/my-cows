services:
  my-cows-app:
    build: .
    container_name: my-cows-app
    restart: unless-stopped
    ports:
      - "8081:5000"
    environment:
      - DB_PATH=/app/data/scores.sqlite
      - PORT=5000
    volumes:
      - ./data:/app/data
